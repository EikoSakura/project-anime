<button type="button" class="badge" data-action="flipBadge" data-key="{{key}}">
  <span class="dia" style="--rkc: var({{cssVar}})"></span>
  <span class="r{{#if flipped}} alt{{/if}}{{#if small}} sm{{/if}}">{{text}}</span>
</button>
