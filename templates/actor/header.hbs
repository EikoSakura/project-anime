<div class="sheet-head">
<header class="obi">
  <div class="obi-inner">
    {{#if editMode}}
    <img class="portrait editable" src="{{img}}" alt="{{name}}" data-edit="img" data-action="editImage"
      data-tooltip="{{localize "PROJECTANIME.Header.EditPortrait"}}">
    {{else}}
    <div class="portrait">
      {{#if portrait}}<img src="{{portrait}}" alt="{{name}}">{{else}}<span class="ph">{{initial}}</span>{{/if}}
    </div>
    {{/if}}
    <div class="idblock">
      <div class="eyebrow">{{localize "PROJECTANIME.Header.Character"}}</div>
      {{#if editMode}}
      <input class="ed-input ed-cname" type="text" name="name" value="{{name}}"
        placeholder="{{localize "PROJECTANIME.Header.Name"}}" aria-label="{{localize "PROJECTANIME.Header.Name"}}">
      {{else}}
      <div class="cname">{{name}}</div>
      {{/if}}
      <div class="cmeta">
        <span>{{localize "PROJECTANIME.Header.Age"}} <b>{{system.age}}</b></span>
        <span><b>{{system.pronouns}}</b></span>
        <span>{{localize "PROJECTANIME.Header.Player"}} <b>{{system.player}}</b></span>
      </div>
    </div>
    <div class="sealwrap">
      <div class="seal" style="--sealc: var({{grade.cssVar}})" aria-label="{{localize "PROJECTANIME.Header.Grade"}}">
        <span class="lbl">{{localize "PROJECTANIME.Header.Grade"}}</span>
        <span class="g">{{grade.letter}}</span>
        <span class="num">{{grade.number}}</span>
      </div>
      {{#if editMode}}
      <div class="sealsteps">
        <button type="button" class="smallbtn" data-action="stepRank" data-kind="grade" data-dir="-1"
          aria-label="{{localize "PROJECTANIME.Edit.RankDown"}}">−</button>
        <button type="button" class="smallbtn" data-action="stepRank" data-kind="grade" data-dir="1"
          aria-label="{{localize "PROJECTANIME.Edit.RankUp"}}">＋</button>
      </div>
      {{/if}}
    </div>
  </div>
</header>
{{> "systems/project-anime/templates/actor/vitals.hbs"}}
</div>
